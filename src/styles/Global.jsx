import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    version: {
        textAlign: 'center',
        marginTop: 16,
        fontSize: 12,
        fontWeight: 'bold',
        color: '#666',
      },
});

export default styles;